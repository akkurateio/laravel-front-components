<section class="section{{!empty($size) ? ' '.$size : ''}}{{!empty($color) ? ' bg-'.$color : ''}}{{!empty($text) ? ' text-'.$text : ''}}">
    <div class="container text-center max-w-2xl mx-auto">
        <p class="text-5xl font-bold mb-0 leading-tight">
            We made it so beautiful and simple. It combines landings, pages, blogs and shop screens.
        </p>
    </div>
</section>
